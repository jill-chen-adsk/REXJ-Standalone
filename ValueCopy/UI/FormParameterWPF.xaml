<Window x:Class="ADSK.JExtRAC.ValueCopy.UI.FormParameterWPF"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ADSK.JExtRAC.ValueCopy.UI"
        Title="FormParameter" Height="510" Width="474"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="8,7,8,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="lvParameters" 
                  Grid.Row="0"
                  BorderThickness="1"
                  BorderBrush="#FFABADB3"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsGroup}" Value="True">
                            <Setter Property="Background" Value="LightGray"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="35"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- パラメータチェックボックス -->
                        <CheckBox Grid.Column="0" 
                                  IsChecked="{Binding IsParameterChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  IsThreeState="{Binding IsGroup}"
                                  Checked="CheckBox_StateChanged"
                                  Unchecked="CheckBox_StateChanged"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"/>

                        <!-- パラメータ名 -->
                        <TextBlock Grid.Column="1" 
                                   Text="{Binding Name}" 
                                   VerticalAlignment="Center"
                                   Margin="5,0,0,0"/>

                        <!-- パラメータ値 (グループ行では非表示) -->
                        <TextBlock Grid.Column="2" 
                                   Text="{Binding Value}" 
                                   VerticalAlignment="Center"
                                   Visibility="{Binding IsGroup, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,8,0,0">
            <Button x:Name="btApply" 
                    Content="btApply" 
                    Width="75" 
                    Height="21" 
                    Margin="0,0,6,0"
                    Click="btApply_Click"/>
            <Button x:Name="btCancel" 
                    Content="btCancel" 
                    Width="75" 
                    Height="21"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window> 